name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: Ubuntu 2-core
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Print goodbye
        run: echo "Done - bye!"
